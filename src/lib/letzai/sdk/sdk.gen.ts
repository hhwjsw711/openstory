// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { ImageEditCreateData, ImageEditCreateErrors, ImageEditCreateResponses, ImageEditFindData, ImageEditFindErrors, ImageEditFindResponses, ImageEditGetData, ImageEditGetErrors, ImageEditGetMaskData, ImageEditGetMaskErrors, ImageEditGetMaskResponses, ImageEditGetResponses, ImagesCreateData, ImagesCreateErrors, ImagesCreateResponses, ImagesFindData, ImagesFindErrors, ImagesFindResponses, ImagesGetData, ImagesGetErrors, ImagesGetResponses, ImagesInterruptData, ImagesInterruptErrors, ImagesInterruptResponses, ImagesPrivacyChangeData, ImagesPrivacyChangeErrors, ImagesPrivacyChangeResponses, ImagesPromptPrivacyChangeData, ImagesPromptPrivacyChangeErrors, ImagesPromptPrivacyChangeResponses, ModelsCreateData, ModelsCreateErrors, ModelsCreateResponses, ModelsDeleteData, ModelsDeleteErrors, ModelsDeleteResponses, ModelsFindData, ModelsFindErrors, ModelsFindResponses, ModelsGetData, ModelsGetErrors, ModelsGetResponses, ModelThumbnailData, ModelThumbnailErrors, ModelThumbnailResponses, ModelUpdateData, ModelUpdateErrors, ModelUpdateResponses, PublicVideoCreateData, PublicVideoCreateErrors, PublicVideoCreateResponses, PublicVideoGetData, PublicVideoGetErrors, PublicVideoGetResponses, PublicVideoListData, PublicVideoListErrors, PublicVideoListResponses, PublicVideosPrivacyChangeData, PublicVideosPrivacyChangeErrors, PublicVideosPrivacyChangeResponses, PublicVideosPromptPrivacyChangeData, PublicVideosPromptPrivacyChangeErrors, PublicVideosPromptPrivacyChangeResponses, UpscaleCreateData, UpscaleCreateErrors, UpscaleCreateResponses, UpscaleDeleteData, UpscaleDeleteErrors, UpscaleDeleteResponses, UpscaleFindData, UpscaleFindErrors, UpscaleFindResponses, UpscaleGetData, UpscaleGetErrors, UpscaleGetResponses, UserImageCreateData, UserImageCreateErrors, UserImageCreateResponses, UserImageDeleteData, UserImageDeleteErrors, UserImageDeleteResponses, UserImageGetData, UserImageGetErrors, UserImageGetResponses, UserImageListData, UserImageListErrors, UserImageListResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Find and filter images
 *
 * Retrieves a paginated list of images.
 *
 * Query Parameters:
 * - page: Page number (default: 1)
 * - limit: Items per page (1-50, default: 10)
 * - sortBy: Field to sort by
 * - sortOrder: "ASC" or "DESC"
 * - userId: Filter by user ID
 * - username: Filter by username
 * - privacy: Filter by privacy level
 * - systemVersion: Filter by system version
 * - isHot: Filter by hot images
 * - includeNsfw: Include NSFW content (default: false)
 * - onlyFollowing: Show only images from followed users
 * - modelIds: Filter by model IDs
 * - status: Filter by status (see Status Values)
 *
 * Status Values:
 * - new: Initial state, waiting to be processed
 * - generating: Currently being generated
 * - ready: Generation completed successfully
 * - hidden: Image has been hidden from public view
 * - failed: Generation failed
 * - interrupted: Generation was interrupted
 * - not_allowed: Generation was rejected
 *
 * Response Headers:
 * - X-Total-Count: Total count of items
 * - X-Current-Page: Current page of pagination
 * - X-Per-Page: Results per page
 * - X-Total-Pages: Total count of pages
 */
export const imagesFind = <ThrowOnError extends boolean = false>(options: Options<ImagesFindData, ThrowOnError>) => (options.client ?? client).get<ImagesFindResponses, ImagesFindErrors, ThrowOnError>({ url: '/images', ...options });

/**
 * Create a new image
 *
 * Creates a new image generation task from a text prompt.
 *
 * Supports both user integration and organization integration tokens.
 *
 * Required Parameters:
 * - prompt: Text description of the desired image
 *
 * Optional Parameters:
 * - width: Image width in pixels (480-2160, default: 1600)
 * - height: Image height in pixels (480-2160, default: 1600)
 * - quality: Generation quality level (1-6, default: 2)
 * - creativity: Creative variation level (1-6, default: 2)
 * - hasWatermark: Apply watermark (default: true)
 * - baseModel: Base model identifier used for generation (max 255 characters)
 * - systemVersion: AI system version (2 or 3)
 * - mode: Generation mode ("default" or "sigma")
 * - organizationId: Organization UUID to deduct credits from (user must be a member)
 *
 * Credit Deduction:
 * - If organizationId is provided, credits will be deducted from the organization account
 * - If organizationId is not provided, credits will be deducted from the user account
 * - User must be a member of the organization to use organization credits
 *
 * Monitor the generation status using the GET /images/{id} endpoint.
 */
export const imagesCreate = <ThrowOnError extends boolean = false>(options: Options<ImagesCreateData, ThrowOnError>) => (options.client ?? client).post<ImagesCreateResponses, ImagesCreateErrors, ThrowOnError>({
    url: '/images',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get detailed image information
 *
 * Retrieves information about a specific image by ID.
 *
 * Path Parameters:
 * - id: UUID of the image
 */
export const imagesGet = <ThrowOnError extends boolean = false>(options: Options<ImagesGetData, ThrowOnError>) => (options.client ?? client).get<ImagesGetResponses, ImagesGetErrors, ThrowOnError>({ url: '/images/{id}', ...options });

/**
 * Interrupt image generation
 *
 * Stops the ongoing generation process for a specific image.
 *
 * Path Parameters:
 * - id: UUID of the image to interrupt
 *
 * The endpoint will:
 * - Stop the generation process
 * - Update image status to interrupted
 *
 * Note: Only works for images in generating status.
 */
export const imagesInterrupt = <ThrowOnError extends boolean = false>(options: Options<ImagesInterruptData, ThrowOnError>) => (options.client ?? client).put<ImagesInterruptResponses, ImagesInterruptErrors, ThrowOnError>({ url: '/images/{id}/interruption', ...options });

/**
 * Update image privacy
 *
 * Changes the privacy settings of an image.
 *
 * Path Parameters:
 * - id: UUID of the image
 *
 * Request Body:
 * - privacy: New privacy level (public, private, organization)
 *
 * Note: Only works for images in ready status.
 */
export const imagesPrivacyChange = <ThrowOnError extends boolean = false>(options: Options<ImagesPrivacyChangeData, ThrowOnError>) => (options.client ?? client).put<ImagesPrivacyChangeResponses, ImagesPrivacyChangeErrors, ThrowOnError>({
    url: '/images/{id}/privacy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update image prompt privacy
 *
 * Changes the prompt privacy settings of an image.
 *
 * Path Parameters:
 * - id: UUID of the image
 *
 * Request Body:
 * - hidden: Boolean indicating if the prompt should be hidden
 *
 * Note: Only works for images owned by the user.
 */
export const imagesPromptPrivacyChange = <ThrowOnError extends boolean = false>(options: Options<ImagesPromptPrivacyChangeData, ThrowOnError>) => (options.client ?? client).put<ImagesPromptPrivacyChangeResponses, ImagesPromptPrivacyChangeErrors, ThrowOnError>({
    url: '/images/{id}/prompt-privacy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Find image edits
 *
 * Retrieves a paginated list of image edits.
 *
 * Query Parameters:
 * - page: Page number (default: 1)
 * - limit: Items per page (default: 20)
 * - sortBy: Field to sort by (createdAt, status)
 * - sortOrder: "ASC" or "DESC"
 * - status: Filter by status (see Status Values)
 *
 * Status Values:
 * - new: Edit task created but not started
 * - generating: Currently processing the edit
 * - ready: Edit completed successfully
 * - saved: Edit has been saved by user
 * - failed: Edit failed to process
 * - interrupted: Edit was stopped by user
 *
 * Response Headers:
 * - X-Total-Count: Total count of items
 * - X-Current-Page: Current page of pagination
 * - X-Per-Page: Results per page
 * - X-Total-Pages: Total count of pages
 */
export const imageEditFind = <ThrowOnError extends boolean = false>(options: Options<ImageEditFindData, ThrowOnError>) => (options.client ?? client).get<ImageEditFindResponses, ImageEditFindErrors, ThrowOnError>({ url: '/image-edits', ...options });

/**
 * Create a new image edit
 *
 * Creates an image edit task that modifies an existing image using inpainting, outpainting, or skin editing.
 *
 * Required Parameters:
 * - mode: "in" for inpainting (modify within image), "out" for outpainting (extend image), "skin" for skin editing, or "context" for context editing
 *
 * Optional Parameters:
 * - originalImageCompletionIds: Array of UUIDs of original image completions
 * - inputImageUrls: Array of source image URLs to edit
 * - prompt: Text description of the desired modifications
 * - baseModel: Base model identifier for generation (max 255 characters)
 * - mask: Base64 encoded image mask (required for inpainting)
 * - width/height: Target dimensions for the edited image in pixels
 * - imageCompletionsCount: Number of variations to generate (1-5, default: 3)
 * - organizationId: Organization UUID to deduct credits from (user must be a member)
 * - settings: Additional generation settings object
 * - webhookUrl: Optional URL to receive a POST notification when upscale is complete
 *
 * Credit Deduction:
 * - If organizationId is provided, credits will be deducted from the organization account
 * - If organizationId is not provided, credits will be deducted from the user account
 * - User must be a member of the organization to use organization credits
 *
 * Response includes:
 * - originalImageCompletions: Array of original image completions used as source
 * - originalImageCompletionIds: Array of UUIDs of original image completions
 *
 * Monitor the edit status using the GET /image-edits/{id} endpoint.
 */
export const imageEditCreate = <ThrowOnError extends boolean = false>(options: Options<ImageEditCreateData, ThrowOnError>) => (options.client ?? client).post<ImageEditCreateResponses, ImageEditCreateErrors, ThrowOnError>({
    url: '/image-edits',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get an image edit
 *
 * Retrieves a specific image edit by ID.
 *
 * Path Parameters:
 * - id: UUID of the image edit
 *
 *
 */
export const imageEditGet = <ThrowOnError extends boolean = false>(options: Options<ImageEditGetData, ThrowOnError>) => (options.client ?? client).get<ImageEditGetResponses, ImageEditGetErrors, ThrowOnError>({ url: '/image-edits/{id}', ...options });

/**
 * Get mask for image completion
 *
 * Retrieves the mask associated with an image completion.
 *
 * Path Parameters:
 * - imageCompletionId: UUID of the image completion
 *
 * Returns the mask data that was used for the image edit. This endpoint is useful for retrieving the original mask used in inpainting operations.
 */
export const imageEditGetMask = <ThrowOnError extends boolean = false>(options: Options<ImageEditGetMaskData, ThrowOnError>) => (options.client ?? client).get<ImageEditGetMaskResponses, ImageEditGetMaskErrors, ThrowOnError>({ url: '/image-edits/{imageCompletionId}/mask', ...options });

/**
 * Find models
 *
 * Retrieves a list of models based on search criteria.
 *
 * Query Parameters:
 * - page: Page number
 * - limit: Items per page
 * - sortBy: Field to sort by
 * - sortOrder: Sort direction (ASC, DESC)
 * - search: General search term
 * - name: Filter by model name
 * - userId: Filter by user ID
 * - username: Filter by username
 * - class: Filter by model class
 * - type: Filter by model type
 * - description: Filter by model description
 * - privacy: Filter by privacy level (public, private, licensed)
 * - systemVersion: Filter by system version
 * - isActive: Filter by active status
 * - status: Filter by status (see Status Values)
 *
 * Status Values:
 * - new: Initial state after creation
 * - pending: Waiting for training to start
 * - training: Currently being trained
 * - finished: Training completed
 * - available: Ready for public use
 * - failed: Training failed
 *
 * Response Headers:
 * - X-Total-Count: Total count of items
 * - X-Current-Page: Current page of pagination
 * - X-Per-Page: Results per page
 * - X-Total-Pages: Total count of pages
 */
export const modelsFind = <ThrowOnError extends boolean = false>(options?: Options<ModelsFindData, ThrowOnError>) => (options?.client ?? client).get<ModelsFindResponses, ModelsFindErrors, ThrowOnError>({ url: '/models', ...options });

/**
 * Create a new model
 *
 * Creates a new AI model for image generation.
 *
 * Request Body Parameters:
 * - name: Model identifier (alphanumeric, underscores, dots)
 * - privacy: Access level (public, private, organization)
 * - type: Model type classification
 * - class: Model category
 * - description: Optional description of the model
 * - website: Optional website URL
 * - settings: Optional array of settings (NO_ADULT_CONTENT, NO_VIOLENCE, etc)
 * - trainingDataUrls: Array of image URLs for training (1-50 URLs required)
 * - webhookUrl: Optional URL to receive a POST notification when training is complete
 * - trainingMode: Optional training mode (e.g., "default", "sigma")
 * - organizationId: Organization UUID to deduct credits from (user must be a member)
 *
 * Credit Deduction:
 * - If organizationId is provided, credits will be deducted from the organization account
 * - If organizationId is not provided, credits will be deducted from the user account
 * - User must be a member of the organization to use organization credits
 *
 * Note: Training process starts automatically after creation.
 */
export const modelsCreate = <ThrowOnError extends boolean = false>(options: Options<ModelsCreateData, ThrowOnError>) => (options.client ?? client).post<ModelsCreateResponses, ModelsCreateErrors, ThrowOnError>({
    url: '/models',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a model
 *
 * Permanently deletes a model.
 *
 * Path Parameters:
 * - id: UUID of the model to delete
 */
export const modelsDelete = <ThrowOnError extends boolean = false>(options: Options<ModelsDeleteData, ThrowOnError>) => (options.client ?? client).delete<ModelsDeleteResponses, ModelsDeleteErrors, ThrowOnError>({ url: '/models/{id}', ...options });

/**
 * Get a model
 *
 * Retrieves detailed information about a specific model.
 *
 * Path Parameters:
 * - id: UUID of the model to retrieve
 *
 */
export const modelsGet = <ThrowOnError extends boolean = false>(options: Options<ModelsGetData, ThrowOnError>) => (options.client ?? client).get<ModelsGetResponses, ModelsGetErrors, ThrowOnError>({ url: '/models/{id}', ...options });

/**
 * Update model information
 *
 * Updates the information of an existing model.
 *
 * Path Parameters:
 * - id: UUID of the model
 *
 * Request Body:
 * - privacy: New privacy setting (public, private, licensed)
 * - type: New model type
 * - description: New model description
 * - website: New website URL
 * - settings: New model settings
 */
export const modelUpdate = <ThrowOnError extends boolean = false>(options: Options<ModelUpdateData, ThrowOnError>) => (options.client ?? client).patch<ModelUpdateResponses, ModelUpdateErrors, ThrowOnError>({
    url: '/models/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update model thumbnail
 *
 * Updates the thumbnail of a model.
 *
 * Path Parameters:
 * - id: UUID of the model
 *
 * Request Body:
 * - imageCompletionId: UUID of the image to use as thumbnail
 */
export const modelThumbnail = <ThrowOnError extends boolean = false>(options: Options<ModelThumbnailData, ThrowOnError>) => (options.client ?? client).put<ModelThumbnailResponses, ModelThumbnailErrors, ThrowOnError>({
    url: '/models/{id}/thumbnail',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Find upscales
 *
 * Retrieves a paginated list of upscales.
 *
 * Query Parameters:
 * - page: Page number (default: 1)
 * - limit: Items per page (1-50, default: 10)
 * - sortBy: Field to sort by
 * - sortOrder: "ASC" or "DESC"
 * - userId: Filter by user ID
 * - username: Filter by username
 * - imageId: Filter by source image ID
 * - onlyFollowing: Show only upscales from followed users
 * - status: Filter by status (see Status Values)
 *
 * Status Values:
 * - new: Initial state, waiting to be processed
 * - generating: Currently being upscaled
 * - ready: Upscale completed successfully
 * - failed: Upscale failed
 *
 * Response Headers:
 * - X-Total-Count: Total count of items
 * - X-Current-Page: Current page of pagination
 * - X-Per-Page: Results per page
 * - X-Total-Pages: Total count of pages
 */
export const upscaleFind = <ThrowOnError extends boolean = false>(options?: Options<UpscaleFindData, ThrowOnError>) => (options?.client ?? client).get<UpscaleFindResponses, UpscaleFindErrors, ThrowOnError>({ url: '/upscale', ...options });

/**
 * Generate a new upscale
 *
 * Creates a new upscale request for an existing image.
 *
 * Request Body:
 * - prompt: Optional text prompt to guide the upscale
 * - mode: Upscale mode (default, natural, sharp, simple, legacy, creative, soft)
 * - imageId: UUID of the source image (required if imageUrl not provided)
 * - imageUrl: Direct URL to the image (required if imageId not provided)
 * - size: Optional size of the upscale
 * - strength: Upscale strength factor from 1 to 5 (default: 1)
 * - organizationId: Organization UUID to deduct credits from (user must be a member)
 * - webhookUrl: Optional URL to receive a POST notification when upscale is complete
 *
 * Credit Deduction:
 * - If organizationId is provided, credits will be deducted from the organization account
 * - If organizationId is not provided, credits will be deducted from the user account
 * - User must be a member of the organization to use organization credits
 */
export const upscaleCreate = <ThrowOnError extends boolean = false>(options: Options<UpscaleCreateData, ThrowOnError>) => (options.client ?? client).post<UpscaleCreateResponses, UpscaleCreateErrors, ThrowOnError>({
    url: '/upscale',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an upscale
 *
 * Deletes an existing upscale request.
 *
 * Path Parameters:
 * - id: UUID of the upscale to delete
 */
export const upscaleDelete = <ThrowOnError extends boolean = false>(options: Options<UpscaleDeleteData, ThrowOnError>) => (options.client ?? client).delete<UpscaleDeleteResponses, UpscaleDeleteErrors, ThrowOnError>({ url: '/upscale/{id}', ...options });

/**
 * Get upscale details
 *
 * Retrieves details of a specific upscale.
 *
 * Path Parameters:
 * - id: UUID of the upscale to retrieve
 */
export const upscaleGet = <ThrowOnError extends boolean = false>(options: Options<UpscaleGetData, ThrowOnError>) => (options.client ?? client).get<UpscaleGetResponses, UpscaleGetErrors, ThrowOnError>({ url: '/upscale/{id}', ...options });

/**
 * List user images
 *
 * Returns a list of images uploaded by the authenticated user.
 *
 * The list is sorted by creation date in descending order, with a maximum of 100 items per page.
 */
export const userImageList = <ThrowOnError extends boolean = false>(options?: Options<UserImageListData, ThrowOnError>) => (options?.client ?? client).get<UserImageListResponses, UserImageListErrors, ThrowOnError>({ url: '/user-images', ...options });

/**
 * Create a new user image
 *
 * Creates a new image entry and generates an upload URL.
 *
 * The response includes an 'uploadUrl' property which is a pre-signed S3 URL that can be used to upload the image file.
 *
 * To upload the image:
 * 1. Make a PUT request to the uploadUrl with the image file as the request body
 * 2. Set the Content-Type header to match the image type (e.g., 'image/jpeg', 'image/png')
 * 3. No authentication is required for this PUT request as the URL is pre-signed
 *
 * Example using curl:
 * ```
 * curl -X PUT -H "Content-Type: image/jpeg" --data-binary "@/path/to/image.jpg" "https://pre-signed-url-from-response"
 * ```
 *
 * Example using JavaScript fetch:
 * ```javascript
 * const response = await fetch(uploadUrl, {
 * method: 'PUT',
 * headers: {
 * 'Content-Type': 'image/jpeg'
 * },
 * body: imageFile // File or Blob object
 * });
 * ```
 *
 * After successful upload, the image will be accessible via the 'imageUrl' property in the response.
 */
export const userImageCreate = <ThrowOnError extends boolean = false>(options: Options<UserImageCreateData, ThrowOnError>) => (options.client ?? client).post<UserImageCreateResponses, UserImageCreateErrors, ThrowOnError>({
    url: '/user-images',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a user image
 *
 * Deletes a user image from your library.
 *
 * Path Parameters:
 * - id: UUID of the image to delete
 */
export const userImageDelete = <ThrowOnError extends boolean = false>(options: Options<UserImageDeleteData, ThrowOnError>) => (options.client ?? client).delete<UserImageDeleteResponses, UserImageDeleteErrors, ThrowOnError>({ url: '/user-images/{id}', ...options });

/**
 * Get a user image
 *
 * Returns details of a specific image from your library.
 *
 * Path Parameters:
 * - id: UUID of the image
 */
export const userImageGet = <ThrowOnError extends boolean = false>(options: Options<UserImageGetData, ThrowOnError>) => (options.client ?? client).get<UserImageGetResponses, UserImageGetErrors, ThrowOnError>({ url: '/user-images/{id}', ...options });

/**
 * List videos (public API)
 *
 * Retrieves a paginated list of videos.
 *
 * Optional Query Parameters:
 * - page: Page number (default: 1)
 * - limit: Items per page (1-50, default: 10)
 * - sortBy: Field to sort by
 * - sortOrder: "ASC" or "DESC"
 * - userId: Filter by user ID
 * - username: Filter by username
 * - status: Filter by video status
 * - width: Filter by video width
 * - height: Filter by video height
 * - resolution: Filter by video resolution
 * - prompt: Filter by prompt text
 * - hidePrompt: Filter by hidden prompts
 *
 * Note: Only videos based on public, ready, non-NSFW images are returned.
 */
export const publicVideoList = <ThrowOnError extends boolean = false>(options?: Options<PublicVideoListData, ThrowOnError>) => (options?.client ?? client).get<PublicVideoListResponses, PublicVideoListErrors, ThrowOnError>({ url: '/videos', ...options });

/**
 * Create a new video (public API)
 *
 * Creates a new video generation request.
 *
 * Required Parameters:
 * - prompt: Text description for the video
 * - Either originalImageCompletionId, imageUrl or imageUrls must be provided
 *
 * Optional Parameters:
 * - organizationId: Organization ID for organization accounts
 * - width: Video width in pixels
 * - height: Video height in pixels
 * - resolution: Video resolution
 * - baseModel: Base model identifier for video generation (max 255 characters)
 * - webhookUrl: URL to receive progress notifications
 * - hidePrompt: Whether to hide prompt in public listings (default: false)
 * - settings: Additional generation settings
 *
 * Authentication: Requires valid JWT token.
 *
 * Rate Limits: Subject to user account limits.
 */
export const publicVideoCreate = <ThrowOnError extends boolean = false>(options: Options<PublicVideoCreateData, ThrowOnError>) => (options.client ?? client).post<PublicVideoCreateResponses, PublicVideoCreateErrors, ThrowOnError>({
    url: '/videos',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get video by ID (public API)
 *
 * Retrieves a specific video by its UUID.
 *
 * Required Parameters:
 * - id: UUID of the video
 *
 * Note: Only videos based on public, ready, non-NSFW images are accessible.
 */
export const publicVideoGet = <ThrowOnError extends boolean = false>(options: Options<PublicVideoGetData, ThrowOnError>) => (options.client ?? client).get<PublicVideoGetResponses, PublicVideoGetErrors, ThrowOnError>({ url: '/videos/{id}', ...options });

/**
 * Update video privacy
 *
 * Changes the privacy settings of a video.
 *
 * Path Parameters:
 * - id: UUID of the video
 *
 * Request Body:
 * - privacy: New privacy level (public, private, organization)
 *
 * Note: Only works for videos owned by the user.
 */
export const publicVideosPrivacyChange = <ThrowOnError extends boolean = false>(options: Options<PublicVideosPrivacyChangeData, ThrowOnError>) => (options.client ?? client).put<PublicVideosPrivacyChangeResponses, PublicVideosPrivacyChangeErrors, ThrowOnError>({
    url: '/videos/{id}/privacy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update video prompt privacy
 *
 * Changes the prompt privacy settings of a video.
 *
 * Path Parameters:
 * - id: UUID of the video
 *
 * Request Body:
 * - hidePrompt: Boolean indicating if the prompt should be hidden
 *
 * Note: Only works for videos owned by the user.
 */
export const publicVideosPromptPrivacyChange = <ThrowOnError extends boolean = false>(options: Options<PublicVideosPromptPrivacyChangeData, ThrowOnError>) => (options.client ?? client).put<PublicVideosPromptPrivacyChangeResponses, PublicVideosPromptPrivacyChangeErrors, ThrowOnError>({
    url: '/videos/{id}/prompt-privacy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
